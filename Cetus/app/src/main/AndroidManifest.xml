<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cridacamo.cetus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.RegisterAssistantActivity"></activity>
        <activity android:name=".views.AssistantsActivity" />
        <activity android:name=".views.RegisterUserActivity" />
        <activity android:name=".views.RegisterEventActivity" />
        <activity android:name=".views.EventsActivity" />
        <activity android:name=".views.UserProfileActivity" />
        <activity android:name=".views.MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>